---
interface Props {
  variant?: 'dark' | 'light';
  class?: string;
}

const { class: className = '' } = Astro.props;
const base = import.meta.env.BASE_URL;
const src = `${base}images/logo/logo.png`;
---

<img src={src} alt="Algum Ritmo — Projeto Acústico" class={`site-logo ${className}`} />
